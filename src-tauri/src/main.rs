// ============================================================
// main.rs — Точка входа приложения vlessok
// ============================================================
// Этот файл запускает Tauri-приложение.
// Вся логика находится в lib.rs — здесь только вызов run().
// ============================================================

// Эта строка убирает чёрное консольное окно в релизной сборке.
// В дебаге (dev-режиме) консоль будет видна — это нормально и полезно.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    // Запускаем приложение
    vlessok_lib::run()
}
